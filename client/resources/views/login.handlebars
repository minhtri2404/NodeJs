<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
    body {
        background-color: #f0f8ff; /* Nền màu xanh sáng */
        color: #333333; /* Chữ màu tối */
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        margin: 0;
    }

    .login-container {
        background-color: #ffffff; /* Màu nền form trắng */
        padding: 30px;
        border-radius: 12px;
        box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 380px;
    }

    .form-control {
        background-color: #e0f7fa; /* Màu nền ô input xanh nhạt */
        color: #333333; /* Màu chữ tối */
        border: 1px solid #81d4fa; /* Đường viền xanh dương nhạt */
        border-radius: 8px;
        padding: 12px;
        margin-bottom: 15px;
        width: 100%;
    }

    .form-control::placeholder {
        color: #00796b; /* Màu chữ placeholder xanh đậm */
    }

    .btn-login {
        background-color: #26c6da; /* Màu nút đăng nhập xanh dương sáng */
        color: white;
        border: none;
        padding: 12px;
        border-radius: 8px;
        width: 100%;
        cursor: pointer;
    }

    .btn-login:hover {
        background-color: #00acc1; /* Màu khi hover (xanh dương đậm) */
    }

    .btn-login:active {
        background-color: #0097a7; /* Màu khi nhấn */
    }
</style>


</head>
<body>
    <div class="login-container">
        <h2 class="text-center">Đăng nhập</h2>
        <form action="/auth/login" method="POST">
            <div class="mb-3">
                <label for="username" class="form-label">Tên đăng nhập:</label>
                <input type="text" id="username" name="username" class="form-control" placeholder="Nhập tên đăng nhập" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Mật khẩu:</label>
                <input type="password" id="password" name="password" class="form-control" placeholder="Nhập mật khẩu" required>
            </div>
            <button type="submit" class="btn btn-login w-100">Đăng nhập</button>
        </form>
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
