<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        body {
            background-color: #f0f8ff; /* Nền sáng với màu xanh nhạt */
            color: #333; /* Màu chữ tối cho dễ đọc */
            font-family: 'Arial', sans-serif;
        }
        .sidebar {
            height: 100vh;
            width: 250px;
            position: fixed;
            background: #0066cc; /* Màu nền sidebar là xanh dương */
            padding-top: 20px;
            color: white;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
        }
        .sidebar a {
            padding: 12px;
            display: block;
            color: white;
            text-decoration: none;
            font-size: 16px;
        }
        .sidebar a:hover {
            background: #004d99; /* Màu khi hover sẽ tối hơn */
            border-radius: 5px;
        }
        .content {
            margin-left: 260px;
            padding: 20px;
        }
        .card {
            background: #ffffff; /* Nền trắng cho card */
            border: 1px solid #e0e0e0; /* Viền nhẹ cho card */
            border-radius: 10px;
            color: #333; /* Màu chữ tối cho dễ đọc */
            padding: 15px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Hiệu ứng bóng cho card */
        }
        .card i {
            font-size: 30px;
            margin-bottom: 10px;
        }
        .card-blue { background: #007bff; color: white; }
        .card-green { background: #28a745; color: white; }
        .card-yellow { background: #ffc107; color: black; }
        .card h5 {
            font-size: 18px;
            margin-bottom: 10px;
        }
        .card h3 {
            font-size: 24px;
            font-weight: bold;
        }
        .table {
            color: #333;
            border: 1px solid #ccc;
        }
        .table thead {
            background: #007bff; /* Nền xanh cho tiêu đề bảng */
            color: white;
        }
        .table tbody tr:hover {
            background: #e6f2ff; /* Hiệu ứng hover nền sáng */
        }
        th, td {
            text-align: center;
            padding: 12px;
            border: 1px solid #ccc;
        }
        .badge {
            font-size: 14px;
            padding: 5px 10px;
        }
        /* Responsive Layout */
        @media (max-width: 768px) {
            .content {
                margin-left: 0;
            }
            .card {
                margin-bottom: 20px;
            }
            .sidebar {
                width: 200px;
            }
        }
    </style>
</head>
<body>

    <div class="content">
        <h2 class="mb-4">Dashboard</h2>
        <!-- Cards -->
        <div class="row">
            <div class="col-md-4">
                <div class="card card-blue">
                    <i class="fas fa-box"></i>
                    <h5>Tổng số sản phẩm</h5>
                    <h3>120</h3>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card card-green">
                    <i class="fas fa-shopping-cart"></i>
                    <h5>Tổng số đơn hàng</h5>
                    <h3>45</h3>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card card-yellow">
                    <i class="fas fa-dollar-sign"></i>
                    <h5>Doanh thu</h5>
                    <h3>$5,200</h3>
                </div>
            </div>
        </div>

        <!-- Table Example -->
        <div class="table-responsive mt-5">
            <table class="table">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Tên Sản Phẩm</th>
                        <th>Giá</th>
                        <th>Trạng Thái</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>1</td>
                        <td>Sản phẩm A</td>
                        <td>$20</td>
                        <td><span class="badge bg-success">Còn hàng</span></td>
                    </tr>
                    <tr>
                        <td>2</td>
                        <td>Sản phẩm B</td>
                        <td>$25</td>
                        <td><span class="badge bg-warning">Hết hàng</span></td>
                    </tr>
                    <tr>
                        <td>3</td>
                        <td>Sản phẩm C</td>
                        <td>$30</td>
                        <td><span class="badge bg-success">Còn hàng</span></td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
